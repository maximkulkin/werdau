
$('table.client-sortable').ready(function() {
  var table = $(this)

  table.find('tbody').sortable({
    handle: '.handle',
    items: 'tr',
    update: function() {
      table.find('input[name$="[position]"]').each(function(i, e) { $(e).val(i+1) })
    }
  })
})

