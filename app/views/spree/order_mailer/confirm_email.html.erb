<html>
<head>
  <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
  <title>Спасибо за Ваш заказ!</title>
  <style type="text/css" media="screen">
      body {font-family: "segoe ui", "arial", "tahoma", sans-serif;
          color: black;
          line-height: 1.3;
            }
    body,  h1, h2, p {margin: 0; padding: 0;}
.order-items table {
  width: 100%;
}

.order-items a {
  color: #4c6d80;
}

.order-items thead td {
  background: #3f8bab;
  padding: 2px 10px;
  height: 27px;
  line-height: 27px;
  background: url(<%= root_url %><%= image_path 'store/header3.png' %>) repeat-x top;
  position: relative;
}

.order-items thead .lc {
  position: absolute;
  top: 0;
  left: -3px;
  display: block;
  width: 3px;
  height: 31px;
  background: url(<%= root_url %><%= image_path 'store/lc4.png' %>) no-repeat left;
}

.order-items thead .rc {
  position: absolute;
  top: 0;
  right: -3px;
  display: block;
  width: 3px;
  height: 31px;
}

.order-items tbody td {
  border-bottom: 1px solid #ccccd5;
  padding: 2px 10px;
}

.order-items tbody tr:hover {
  background: #f5f9fb;
}

.order-items tr.total td {
  background: #496a7d;
  color: #fff;
  border-top: 1px solid #fff;
  padding-bottom: 5px;
}

.order-items tr.total .cost {
  font-size: 18px;
}

.order-items {
    font-size:13px;
    width:560px;
}
table {border-collapse: collapse;}
    a {color:#ff0000;}
    #top {font-size: 24px; color: white;  border-bottom-style:solid; border-bottom-color:#ff0000;}
    #main {border-bottom-style:solid; border-bottom-color:#ff0000;}
  </style>
</head>
<body>

    <table width="100%" cellpadding="0" cellspacing="0" bgcolor="#e4e4e4">
        <tr>
            <td>
              <table id="top" cellpadding="20" cellspacing="0" width="600" align="center" background= "<%= root_url %><%= image_path 'store/headbg.png' %>">
                    <tr>
                      <td><a href="<%= root_url %>"><img alt="Магазин электроники Werdau.info" src="<%= root_url %><%= image_path 'store/logo.png' %>" /></a></td>
                        <td>Спасибо за Ваш заказ!<br/>т. (8452) 25-49-90</td>
                    </tr>

                </table>
                <table id="main" cellpadding="20" cellspacing="0" width="600" align="center" bgcolor="#fff">
                    <tr align="left">
                      <th>Ваш заказ:</th>
                    </tr>
                    <tr><td>
  <table class="order-items">
    <thead>
      <tr>
        <td><div class="lc"></div>Товар</td>
        <td>Кол-во</td>
        <td class="rt">Цена</td>
        <td><div class="rc"></div>Стоимость</td>
      </tr>
    </thead>

    <tbody>
      <% @order.line_items.each do |item| %>
        <tr>
          <td class="name"><b><%= link_to item.variant.product.name, product_url(item.variant.product) %></b></td>
          <td class="quantity"><%= item.quantity %></td>
          <td class="price"><%= item.price %></td>
          <td class="cost"><%= item.price * item.quantity %></td>
        </tr>
      <% end %>

      <tr class="total">
        <td colspan="2" align="right">Итого:</td>
        <td class="cost"><%= @order.total %></td>
        <td></td>
      </tr>
    </tbody>
  </table>
  </td>
      <table id="bottom" cellpadding="20" cellspacing="0" width="600" align="center">
                    <tr>
                      <td align="center"><%= link_to Spree::Config[:site_url], root_url %></td>
                    </tr>

                </table>
            </td>
        </tr>
    </table>


</body>
</html>
